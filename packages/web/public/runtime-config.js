// Standalone/static hosts use the web build's feature defaults.
// The control plane overrides this path with uncached deployment configuration.
// Keep this file out of the service-worker precache.
