# Code owners for this repository.
#
# CODEOWNERS review is only *enforced* by a branch ruleset. The repo is public
# and scripts/protect-repo.sh now applies its rulesets for real, so turning this
# from advisory into required is a one-flag change there
# (`require_code_owner_review`).

* @pettersj
